
def transpose(matrix):
    # Используем функцию zip() для объединения элементов
    # из каждого списка в кортежи
    # Затем преобразуем кортежи в списки и добавляем их в новую матрицу
    return [list(row) for row in zip(*matrix)]